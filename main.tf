#azure provider block
provider "azurerm" {
  features {}
}

#azure provider block
provider "azurerm" {
  features {}
}

#aws provider block
provider "aws" {
  region = var.aws_region_1
}

#aws provider block
provider "aws" {
  region = var.aws_region_2
}